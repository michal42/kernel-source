From: Jingoo Han <jg1.han@samsung.com>
Date: Fri, 18 Oct 2013 09:21:54 +0900
Subject: [PATCHv2 12/28] net: tg3: remove unnecessary pci_set_drvdata()
Patch-mainline: v3.13-rc1
Git-commit: 4fc76e6f347eed69bf7e2bcbcf9e2c0fd2964ff7
References: bnc#852650 FATE#315916

The driver core clears the driver data to NULL after device_release
or on probe failure. Thus, it is not needed to manually clear the
device driver data to NULL.

Signed-off-by: Jingoo Han <jg1.han@samsung.com>
Signed-off-by: David S. Miller <davem@davemloft.net>
Acked-by: Cho, Yu-Chen <acho@suse.com>
---
 drivers/net/ethernet/broadcom/tg3.c |    2 --
 1 file changed, 2 deletions(-)

--- a/drivers/net/ethernet/broadcom/tg3.c
+++ b/drivers/net/ethernet/broadcom/tg3.c
@@ -17704,7 +17704,6 @@ err_out_free_res:
 err_out_disable_pdev:
 	if (pci_is_enabled(pdev))
 		pci_disable_device(pdev);
-	pci_set_drvdata(pdev, NULL);
 	return err;
 }
 
@@ -17736,7 +17735,6 @@ static void tg3_remove_one(struct pci_de
 		free_netdev(dev);
 		pci_release_regions(pdev);
 		pci_disable_device(pdev);
-		pci_set_drvdata(pdev, NULL);
 	}
 }
 
