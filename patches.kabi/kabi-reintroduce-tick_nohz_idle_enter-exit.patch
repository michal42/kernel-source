From: Jiri Slaby <jslaby@suse.cz>
Subject: kABI: reintroduce tick_nohz_idle_enter/exit
Patch-mainline: never, kabi

In 3.12.37, commit 1ff5b9fb934888276864a6cd98c98d0b07741f78
(tick/powerclamp: Remove tick_nohz_idle abuse), upstream commit
a5fd9733a30d18d7ac23f17080e7e07bb3205b69 removed exports from
tick_nohz_idle_enter/exit. This indeed made the symbol to disappear.

Reintroduce the exports and be done with it.

Signed-off-by: Jiri Slaby <jslaby@suse.cz>
---
 kernel/time/tick-sched.c |    2 ++
 1 file changed, 2 insertions(+)

--- a/kernel/time/tick-sched.c
+++ b/kernel/time/tick-sched.c
@@ -806,6 +806,7 @@ void tick_nohz_idle_enter(void)
 
 	local_irq_enable();
 }
+EXPORT_SYMBOL_GPL(tick_nohz_idle_enter);
 
 /**
  * tick_nohz_irq_exit - update next tick event from interrupt exit
@@ -824,6 +825,7 @@ void tick_nohz_irq_exit(void)
 	else
 		tick_nohz_full_stop_tick(ts);
 }
+EXPORT_SYMBOL_GPL(tick_nohz_idle_exit);
 
 /**
  * tick_nohz_get_sleep_length - return the length of the current sleep
