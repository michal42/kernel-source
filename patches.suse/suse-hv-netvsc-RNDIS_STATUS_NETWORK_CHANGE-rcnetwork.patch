Subject: hv_netvsc: use rcnetwork to restart on RNDIS_STATUS_NETWORK_CHANGE event
From: <ohering@suse.de>
Patch-mainline: Never, this change carry until upstream provides better method

A better change is pending.
---
 drivers/net/hyperv/netvsc_drv.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: linux-3.12-SLE12/drivers/net/hyperv/netvsc_drv.c
===================================================================
--- linux-3.12-SLE12.orig/drivers/net/hyperv/netvsc_drv.c
+++ linux-3.12-SLE12/drivers/net/hyperv/netvsc_drv.c
@@ -794,7 +794,7 @@ static void netvsc_link_change(struct wo
 	struct netvsc_device *net_device;
 	struct rndis_device *rdev;
 	bool notify, refresh = false;
-	char *argv[] = { "/etc/init.d/network", "restart", NULL };
+	char *argv[] = { "/sbin/rcnetwork", "restart", NULL };
 	char *envp[] = { "HOME=/", "PATH=/sbin:/usr/sbin:/bin:/usr/bin", NULL };
 
 	rtnl_lock();
